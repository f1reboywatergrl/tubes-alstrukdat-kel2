#include "checkorder.c"

int main(){
    int Order = 1;
    int Build = 3;
    int Invoice = 3500;

    Queue Komponen = QCreateEmpty();
    for (int i=0; i<length; i++){
        Komponen = ();
        QAdd(&Komponen, ElKomponen, i);
    }

}